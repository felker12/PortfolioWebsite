name: Deploy Legacy .NET Portfolio to Azure

on:
  push:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: windows-latest # Required for .NET Framework 4.8

    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v1

      - name: Restore Packages
        run: nuget restore PortfolioWebsite.sln

      - name: Build and Publish
        run: |
          msbuild PortfolioWebsite.csproj /p:Configuration=Release /p:DeployOnBuild=true /p:PublishUrl="${{ github.workspace }}\publish" /p:WebPublishMethod=FileSystem /p:PackageLocation="${{ github.workspace }}\publish"

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'PortfolioWebsite20250804050941'
          publish-profile: ${{ secrets.AZURE_PORTFOLIO_PUBLISH_PROFILE }}
          package: '${{ github.workspace }}\publish'